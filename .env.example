# Next
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Payload
PAYLOAD_SECRET=replace-me-with-a-long-random-string
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3001

# Auth.js (NextAuth v5)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-me-with-a-long-random-string

# Database (PostgreSQL)
# Use a PgBouncer pool URL for runtime queries where possible.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/aircon?schema=public
# Direct connection (optional) for Prisma migrations.
DIRECT_URL=postgresql://postgres:postgres@localhost:5432/aircon?schema=public

# Cloudinary (optional)
CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME

# Vercel Blob (optional)
BLOB_READ_WRITE_TOKEN=
